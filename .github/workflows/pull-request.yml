name: pull-request
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]

jobs:
  code:
    uses: unional/.github/.github/workflows/pnpm-verify.yml@main
    with:
      os: ubuntu-latest